def main() -> None:
    print("Hello, world!")


if __name__ == "__main__":
    # Do not put any other code here, as poetry's `brainfuck` entrypoint
    # executes the `main` function directly (i.e. anything out of `main` will
    # not be executed if running `$ poetry run brainfuck`).
    main()
